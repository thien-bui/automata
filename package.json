{
  "name": "automata",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo \"TODO: implement build\"",
    "lint": "echo \"TODO: implement lint\"",
    "test": "npm run test --workspace=@automata/api && npm run test --workspace=@automata/web",
    "test:e2e": "npm run test:e2e --workspace=@automata/web",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
