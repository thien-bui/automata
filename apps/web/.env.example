# --- Vite dev server ----------------------------------------------------
# Proxy target for API requests during local development.
VITE_DEV_API_URL=http://127.0.0.1:4000

# --- Client defaults ----------------------------------------------------
# Browser-exposed Google Maps key used by the map preview component.
VITE_GOOGLE_MAPS_BROWSER_KEY=your-google-maps-browser-key

# Default freshness (seconds) for cached API data on the dashboard.
VITE_DEFAULT_FRESHNESS=300
# Discord widget-specific cache freshness override.
VITE_DEFAULT_DISCORD_FRESHNESS=300
# Discord widget refresh controls (seconds).
VITE_DEFAULT_DISCORD_REFRESH=300
VITE_MIN_DISCORD_REFRESH=60
VITE_MAX_DISCORD_REFRESH=3600

# Weather alert threshold temperature in Fahrenheit.
VITE_DEFAULT_ALERT_THRESHOLD=45

# Default coordinates used for map previews and weather lookups.
VITE_DEFAULT_LATITUDE=47.6062
VITE_DEFAULT_LONGITUDE=-122.3321

# --- Testing ------------------------------------------------------------
# Override the base URL used by Playwright E2E tests.
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5173
# Custom command for spinning up the web app before Playwright runs.
PLAYWRIGHT_WEB_SERVER=
# Set to 1 inside CI environments to enable retries/reporters.
CI=
